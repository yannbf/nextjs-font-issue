// TODO: This file can just not exist and it's possible. Storybook should not fail then.
// Maybe we do: next.config.js file not found, using default config instead... or something like that.

/**
 * @type {import('next').NextConfig}
 */
 const nextConfig = {
  /* config options here */
}

module.exports = nextConfig